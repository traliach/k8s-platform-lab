import React from 'react';
import { createRoot } from 'react-dom/client';
import GrowStrongMealPlan from './GrowStrongMealPlan.jsx';

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GrowStrongMealPlan />
  </React.StrictMode>
);
